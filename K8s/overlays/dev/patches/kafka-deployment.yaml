apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka
spec:
  template:
    spec:
      containers:
        - name: kafka
          resources:
            requests:
              cpu: "150m"
              memory: "200Mi"
            limits:
              cpu: "300m"
              memory: "400Mi"
