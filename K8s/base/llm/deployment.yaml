apiVersion: apps/v1
kind: Deployment
metadata:
  name: llmservice
  labels:
    app: llmservice
    app.kubernetes.io/version: "1.12.0"
    app.kubernetes.io/part-of: opentelemetry-demo
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: llmservice
  template:
    metadata:
      labels:
        app: llmservice
    spec:
      containers:
        - name: llmservice
          image: ghcr.io/open-telemetry/demo:latest-llm

          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: service
          envFrom:
            - configMapRef:
                name: llmservice-config
            - secretRef:
                name: llmservice-secret
          resources:
            requests:
              cpu: "100m"
              memory: "50Mi"
            limits:
              cpu: "200m"
              memory: "100Mi"